{{#each this}}
<h1 class='country_title'>{{name}}</h1>
 <div class='country'>
    <li>
      <img src='{{flag}}' alt='{{this.name}}' width='20px' />
      <p class='item'>{{this.name}}</p>
    </li></div>

{{/each}}
